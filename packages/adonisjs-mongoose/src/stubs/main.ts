/**
 * Absolute path to the directory holding the stub templates at runtime.
 * After build, the stubs are copied alongside this compiled file, so the
 * directory resolves to `build/stubs`.
 */
export const stubsRoot = import.meta.dirname
